# Changelog

本文件记录 dummy 的重要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

**说明**：dummy 的 Phase 0~2 建设期（96 个提交）已有 `docs/roadmap.md` 记录，本文件从 **Phase 3 的工具循环加固**开始，覆盖从 `d2a7683` 到当前的 37 个提交。

---

## [未发布] — Phase 3 工具循环加固

> 时间跨度：2026-09-14 ~ 2026-09-17
> 提交数：37
> 核心变化：**Agent 与模型的职责边界被重新划定**

### 一句话概括

这一阶段做了三轮修正，每一轮都在做同一件事：**把"替模型做判断"的代码改成"向模型提供事实"的代码**。

```
第一轮：验证门（判决者）      → 情况说明（信息提供者）
第二轮：护栏（拦截/暂停）      → 护栏（纯观察者，零拦截）
第三轮：错误判定（猜成败）     → 报告实际值（换行符、成本、环境）
```

---

## 一、新增能力

### 1.1 输出层（`ui.py`，257 行）—— 新增

**拼与印分离**：把"消息是什么"（语义）和"消息怎么画"（表现）拆成两层。

```python
# ① 拼（业务层，返回 Message 对象）
msg = ui.build(KIND_TOOL_CALL, tool=name, args=args)
# ② 印（渲染层，消费 Message）
ui.emit(msg)
# 或一步到位
ui.show(KIND_TOOL_RESULT, preview, tool=name, indent="  ")
# 原样输出（工具内部的交互面板）
ui.raw("即将执行: ...")
```

**三种可插拔渲染器**（`DUMMY_UI` 环境变量切换）：

| 值 | 效果 |
|---|---|
| `plain`（默认） | 原有样式：图标 + 前缀着色，**与改动前视觉完全一致**（可无痛回退） |
| `line` | 竖线 + 分隔线：对话流（工具调用/结果/思考/回复）加 `┃` 前缀，多行逐行加；每轮结束画一条 `─` 横线 |
| `silent` | 静音**对话流**，但**保留 `raw` 输出**（见下方"为什么 raw 不静音"） |

**竖线的边界**：只作用于**对话流**。工具内部的确认面板（`read_file` 的隐私提示、`terminal` 的执行确认、`write_file` 的 diff）保持原样——它们是"问用户话"的交互面板，不是"叙述过程"。

**为什么 `raw` 在 silent 下不静音**：两类输出性质不同。
- 对话流是"给人眼睛看的"，测试时是噪音 → 静音
- `raw` 承载的是**程序 stdout**（`/help`、会话列表、工具确认面板），用户执行 `python main.py > log.txt` 时必须保留，否则文件会变空

**收口范围**：`main.py` 全部 47 处、`core.py` 工具循环 3 处、`tools/` 4 处交互面板。**未收口**：`core.py` 剩余 37 处（注入/压缩/错误提示）、`tools/write_file.py` 23 处（diff 展示）——理由是它们是"拼好的成品文本 + 内部细节"，收口只有形式意义。

### 1.2 环境编码探测（`env_probe.py`，105 行）—— 新增

`terminal` 工具的返回首行从：

```
[SHELL: git-bash]
```

改为：

```
[SHELL: git-bash | os=win32 | console=cp936 | py_io=utf-8 | fs=utf-8]
```

**动机**（实测数据）：一次任务 18 次工具调用里，**11 次**在跟编码搏斗——模型不知道"这台机器的控制台是什么编码"，于是反复调 `chcp` 查代码页、写 `python -c "open(...,'rb').decode(...)"` 探文件编码、写临时文件做 A/B 对照。

**设计红线：探测，不设置。** 绝不 `setenv LANG`、不执行 `chcp`、不改 `sys.stdout`——那会把"当前平台的解法"写死进代码，换到别的系统就是错的。有两条测试专门锁死这条红线（`test_does_not_mutate_environment` / `test_does_not_change_stdout_encoding`）。

**跨平台实现**：

| 平台 | `console` 字段的探测方式 |
|---|---|
| Windows | 从 `chcp` 输出解析（实测返回 `cp936`，与实际一致） |
| POSIX | 从 `LC_ALL` / `LC_CTYPE` / `LANG` 解析 |
| 都失败 | `unknown`（宁说不知道，不给错信息） |

**验证状态（诚实标注）**：Windows 分支**已本机实测**；POSIX 分支**只做了逻辑验证**（monkeypatch 走该分支喂各种 `LANG` 值），**未在真实 Linux 上跑过**——原因是本机 WSL 已知不可用、Docker daemon 未启动。已在模块头与测试注释中标注。

### 1.3 平台约定速查技能（`skills/platform-conventions/`）—— 新增

**设计**：表 + 兜底搜索。已知约定秒查，表里没有的用 `web_search` 查。

**内容**：Windows 写含中文 `.bat` 的二选一方案、CRLF、不要 BOM、`file`/`xxd` 检查法、路径写法、`cmd //c` 双斜杠；Linux/macOS 的 `locale`、LF、`sed` 去 CR；通用原则（编码问题先分"文件内容"与"控制台渲染"两层定位）。

**只放通用约定**，本机特性不放（随使用追加）。

### 1.4 收尾情况说明（`turn_context.py`，170 行）—— 新增（替代 `verify_stop.py`）

模型想收尾时，附一份**陈述式**的情况说明：

```
[本轮运行情况]
本轮改动/新建的文件:
- start-wt.bat
本轮执行记录(按时间顺序,最近 12 条):
- write_file: 写入 start-wt.bat
    → 写入成功: start-wt.bat (24 行, 665 字符) [换行 CRLF]
- terminal: file start-wt.bat
    → [SHELL: git-bash | os=win32 | console=cp936 | ...] DOS batch file, with CRLF

核对上面的记录核对你的说法,不一致就改述;没做/没验的直说,
别写成已完成(需要补验现在还来得及)。**核对的结论用一句话带过就行,
不必逐条复述。**

然后另起一段,用「## 结论」开头,给用户一个最终答复。
**只写用户需要知道的,不要重复你上面已经说过的内容**...
```

**这个模块替代了原来的 `verify_stop.py`**（详见下方"移除"节）。

---

## 二、移除

### 2.1 `verify_stop.py`（346 行）与其 30 条测试 —— 删除

**原因**：它是"收尾验证门"的实现——模型想收尾时，如果本轮改过代码类文件却没拿出"全量测试通过"的证据，就**驳回**（上限 2 次）。它在 `a02d0c5` 之后不再被 `core.py` 调用，**零消费者**。

**为什么必须删而不是留着**：留着会让后续读代码的人以为它还在工作（我自己就被误导过一次，以为它有 5 个消费方，实际只剩 2 个）。

**删除的深层原因**（三个实测 bug）：

| # | 现象 | 根因 |
|---|---|---|
| 1 | 驳回上限"每轮 2 次"被用户插话重置，实际驳回 4 次 | `reset_for_turn()` 在 chat 开头清零，用户插话就开新轮 |
| 2 | `.bat` 文件被要求提供"全量 pytest 证据" → 模型跑了 8 次无关的 pytest | 豁免名单是黑名单式（`.md`/`.txt` 等），`.bat` 不在其中 |
| 3 | 模型的有效验证（`drill.ps1`、对照组、换行检查）全不被认 | 门只认一种证据形态 |

**三个 bug 是同一个病**：**用固定规则枚举不完现实**。

**实测对比**（同一任务：创建一个 Windows Terminal 启动器）：

| | 判决门 | 情况说明 |
|---|---|---|
| 工具调用 | 66 次 | **4 次** |
| 验证门驳回 | 4 次 | **0 次** |
| 轮次用尽中断 | 是 | 否 |
| 关键差别 | 模型看到"你没证据"（注意力被"满足规则"占用） | 模型看到**真实报错**（如 `'…\Git\bin\bash' 不是内部或外部命令`），它能据此判断 |

---

## 三、变更

### 3.1 工具护栏：从"拦截"到"纯观察者"（`tool_guardrails.py`）

**演变过程（两次收紧）**：

```
① 最初：block —— 同一份失败连续 N 次后，不再执行该调用，直到轮次用尽
② 中间：pause —— 拦下这一次，把事实报告给模型
③ 现在：report —— 从不拦截，只在重复到阈值时往工具结果后追加一句事实
```

**为什么最终选择"零拦截"**：循环每轮都会回到模型，**模型看到结果后必须重新决策**——所以不需要代码替它按暂停键。

原文注释（`tool_guardrails.py`）：

> 护栏是**纯观察者**：从不阻止任何调用，只在重复到阈值时往工具结果后追加一句事实描述，由模型下一轮自行判断要不要继续。

**两个信号**：

| 信号 | 判定 |
|---|---|
| 完全相同失败 | 工具 + 参数 + **失败内容**三者都相同，连续出现 |
| 只读调用无进展 | 只读工具的结果与上次完全相同 |

**为什么要求"失败内容也相同"**（这是实测过的坑）：如果只看"同工具同参数"，那"改一处代码 → 跑一次测试"（命令相同、报错每次不同）会被误判成打转并拦死——而那是**正常开发循环**。这个 bug 实测出现过，导致"护栏禁止跑测试、验证门又要求跑通测试"的**互锁**。

**报告文案是事实陈述，不是判决**：

```
[护栏提示] 你已经连续 5 次执行了同一个调用，结果完全相同：
  · 工具：terminal
  · 参数：{"command": "python -m pytest tests/ -q"}
  · 这期间你没有改动任何文件。

（由模型自行判断是否继续）
```

有测试专门断言报告里**不含**"不要/禁止/不许/判定为"这些判决措辞。

### 3.2 工具失败判定：从"猜"到"报事实"（`lessons.py`）

**背景**：`is_tool_error()` 曾经双向误判（实测）：

```
terminal 成功(exit 0):      is_tool_error = True    ← 误判
terminal 成功(ls):          is_tool_error = True    ← 误判
write_file 被取消:          is_tool_error = False   ← 漏判
```

**根因**：它靠**子串匹配**工具输出文本，例如把 `"[EXIT CODE: "` 当失败标记——但 `[EXIT CODE: 0]` 是**成功**。

**修法（三类判据）**：
1. `NOOP_MARKERS`（工具**未执行**的标记，如 `[用户取消]`）→ 算"非成功"
2. `[EXIT CODE: N]` → **解析数字**，非 0 才失败
3. 框架级标记（`[ToolDispatch]` / `[错误]`）

**修后**：探针复测 **0 误判 / 0 漏判**（修前是 2 误判 + 1 漏判）。

### 3.3 `write_file`：返回值报告**实际**换行符

返回值从：

```
写入成功: start-wt.bat (24 行, 665 字符)
```

改为：

```
写入成功: start-wt.bat (24 行, 665 字符) [换行 CRLF]
```

**动机**（实测）：模型写完 `.bat` 后，额外跑了一次 `sed -i 's/\r$//; s/$/\r/'` 去"修"换行符——而那次调用**完全多余**：Python 的 `open(..., "w")` 在 Windows 上本来就做平台转换，写出来已经是 CRLF。

**根因不是能力缺失，是信息缺失**：模型看不到写出来的是什么换行，只能假设"可能是 LF"。

**为什么"读"而不是"假设"**：可以按"Windows 上 Python 会转 CRLF"直接返回 `[CRLF]`，但那是**假设**——一旦 `newline` 参数、Python 版本或调用方式变化，报告就成了假话。读文件头拿到的才是事实。

**能识别四种情况**：`CRLF` / `LF` / `混合(CRLF×N, LF×M)` / `无换行`。混合情况值得单独识别——`line` 模式改单行时最容易产生。

**效果**：下一轮实测中，模型**不再跑多余的 `sed`**。

### 3.4 `system` prompt 稳定化：修复缓存前缀被打碎

**问题**：LLM 的 prompt cache 按**前缀匹配**，system 是消息序列第 0 条——它变一个字节，**后面所有内容**都按未命中计费（缓存价 ¥0.1/M vs 输入价 ¥3.0/M，**差 30 倍**）。

而 dummy 的 system 里有两个每轮都变的字段：

```
prompt.py: "运行时间: 2026-09-16 19:23:46 CST"     ← 精确到秒
core.py:   "当前时间: 2026-09-16 19:31 (Wednesday)" ← 每轮刷新
                ↑ 变更点在第 776 字符,后面 641 字符(cwd + 技能索引)每轮白算
```

**改动**：

| 字段 | 处理 | 理由 |
|---|---|---|
| 运行时间 | **删除** | 僵尸字段（只在建 agent 时算一次，之后永不更新），模型看到"运行时间 19:23"和"当前时间 19:31"只会困惑 |
| 当前目录 | **删除** | 与 `_inject_cwd` 重复（同一个信息写了两遍） |
| 当前时间 | **降精度到天**（`今天是: 2026-09-16`） | 同一天内 system 完全静态 |

**为什么选"降精度"而不是"挪到消息尾部"**（评估了四种）：

| 方案 | 结果 |
|---|---|
| 挪到 system 之后第 1 条 | ❌ **更糟**——它之后的全部内容（含本轮工具结果）都失效 |
| 每轮追加到末尾 | ⚠️ 有效，但时间消息在历史里累积 |
| 就地改写"末尾那条时间消息" | ❌ **错**——下一轮它就不是末尾了，改写它等于改中间 |
| **降精度（采纳）** | ✅ 最小改动，system 完全静态 |

**顺带修复的注入顺序问题**：`_inject_datetime()` 原本是**第一个**被调用的，而各注入函数都是"追加到尾部"——先注入的被后面几个挤到了中间。已把它移到**所有注入之后**执行。

**新增两条不变量测试**：
- `test_system_prompt_is_stable_across_turns`：system 在多轮 chat 之间**逐字节不变**
- `test_volatile_field_is_last_in_system`：会变的内容必须排在所有稳定内容之后

### 3.5 成本计算：修复缓存被算两遍（高估 5.59 倍）

**问题**：

```python
# 旧算法
cost = prompt × ¥3.0 + completion × ¥9.0 + cached × ¥0.1
```

但 **DeepSeek 的 `prompt_tokens` 已包含 `cached_tokens`**（实测：`prompt=1620, cached=1408, miss=212`，`1408+212=1620`）——所以缓存那部分**被算了两遍**。

**修法**：

```python
cost = (prompt - cached) × ¥3.0 + cached × ¥0.1 + completion × ¥9.0
```

**实测高估倍数：5.59 倍**。而且**命中越多高估越狠**——这个 bug 恰好抵消了缓存的收益，让人误以为"缓存没生效"。

**顺带补测试**：`tests/test_usage.py`（6 条）——**此前整个用量/成本模块零测试覆盖**。

### 3.6 `load_dotenv()` 提到 import 之前：修复 `.env` 配置静默失效

**问题**：

```
main.py:40   from ui import ui      ← ui.py 在 import 时读 DUMMY_UI 决定渲染器
main.py:191  load_dotenv()          ← 到这里才加载 .env(太晚)
```

**影响面比 UI 更大**：这个顺序错误让 `.env` 里**所有"import 期生效"的配置**都失效——不只 `DUMMY_UI`，还有 `DUMMY_COLOR`，以及将来任何在 import 时读取的配置。

**为什么一直没被发现**：`DUMMY_API` 这类**运行时读**的变量不受影响。

**修法**：`load_dotenv()` 提到模块顶部，在所有项目模块 import 之前；`main()` 里那处删除。**新增 4 条测试用 AST + 行序检查锁死不变量。**

### 3.7 `llm.chat()` 返回值取值方式 —— 修复 4 处同类 bug

**问题**：三处调用方用同一个错误的模式取内容：

```python
if isinstance(resp, dict):
    content = resp.get("content") or ""
elif resp is not None and hasattr(resp, "get"):
    content = resp.get("content") or ""
else:
    content = ""          # ← 永远走这里
```

`llm.chat()` 返回的是 OpenAI SDK 的 `ChatCompletionMessage`——它**有 `.content`，没有 `.get()`**，所以 `content` 恒为空串。**外层 `except Exception` 又把一切异常吞掉 → 不报错，只是永远不工作。**

**影响面（比 `/improve` 报错严重）**：

| 位置 | 后果 |
|---|---|
| `lessons.generate_lesson` | **教训机制从 Phase 3 Step 3 起从未生效过** |
| `self_improve.generate_proposal` | `/improve` 报"无法生成修复提案" |
| `self_improve._generate_new_content` | 会生成空文件 |

**修法**：新增 `llm.message_content(resp)` 作为**唯一取值入口**（兼容 SDK 对象 / dict / 字符串三种形态），把散落各处的"取内容"动作收口——**它们一起错就是因为各写各的**。

**为什么测试没抓到（关键教训）**：

```
真实:   ChatCompletionMessage        → 有 .content
假对象: type("R", (), {"get": ...})  → 有 .get(),没有 .content
                                        ↑ 测试"恰好能工作"
```

**测试全绿，生产全坏。** 修法同时把测试假对象改成与真实形态一致。

### 3.8 `prompt.py` 第 3、4 条

**第 3 条**（补"结果拿不到时该怎么办"）：

背景实测：模型被要求创建一个启动器，它花了 **12 次调用**反复用 `sed` 替换、`printf` 造测试文件、`wt_probe.bat` 探测——**但它真正要验证的那件事（GUI 窗口有没有弹出来）它一次也验不了**，而它自己也知道（最后说了"沙箱里起不了窗口"）。

原文案只有"出错就换个方式重试"这一个选项，模型就真的去"换个方式"试了 12 次。

改后：

```
3. 工具可能返回错误信息。先分清两种情况：
   - **能拿到结果**（读文件、跑命令）→ 看结果决定下一步...
   - **结果你根本拿不到**（GUI 窗口有没有真的弹出来、需要人手动操作、
     需要你看不见的环境）→ **直接告诉用户"这一步我验证不了，请你确认"**，
     不要反复构造模拟测试去"证明"它...
```

**第 4 条**（一次只调用一个工具）：

背景：实测发现它一个回复里声明多个工具调用（最多一次 4 个 `read_file`），导致用户按 `/p` 打断时"一半执行一半丢弃"。

改后明确要求"**一次只调用一个工具**"，并给了理由（多个调用同时发出时后一个结果会盖住前一个的上下文）与例外（可合并成一个命令）。

**效果**：下一轮实测中，工具调用从 18 次降到 7 次。

---

## 四、修复的工具循环与历史问题

### 4.1 `tool_calls` 配对自愈（修 400 "insufficient tool messages"）

**问题**：一次 Ctrl+C 打断后，`assistant` 声明的 2 个 `tool_calls` 只有 1 个有回复，残缺历史落库；下次加载时原样读回 → **一次损坏永久失效**，每次发 API 都 400。

**四层防线**：

| 层 | 位置 | 作用 |
|---|---|---|
| 出口 | `main.py` 的 `_settle_history` | 退出前补齐残缺配对 |
| 入口 | `core.resume_session()` | 磁盘历史进内存时自愈并回写 |
| **咽喉** | `core.chat()` 两处保险丝 | 压缩前 / 发请求前 |
| 修正 | `/history del` 整组增删 | 删一条 `tool` 回复时连带删父 assistant 与同批兄弟 |

**关键设计决策**：**自愈放 Agent 边界，不放持久层**——塞进 `load_history` 会破坏"忠实读取"契约（实证：跑挂了 `test_compressed_rewrite_consistency`）。持久层只存取，业务约束在 chat 循环/resume/退出处执行。

**修复后**：23 项单元测试 + 13 项端到端测试全通过（用复刻 API 严格校验的假 LLM 证明：残缺历史先被反证为非法，修复后正常发出）。

### 4.2 历史卫生两条边角（P2b）

| 场景 | 处理 |
|---|---|
| 历史尾部是"零回复的悬空块" | **删除**（部分回复的保持补占位，保证进行中的工具循环可续跑） |
| 尾巴停在裸 `tool` 上 | 补一条合成 `assistant` 轮次（`close_interrupted_tool_sequence`） |
| 新增 user 消息 | 走 `_append_user_turn` **单点收口**，顺带兜 `tool → user` 角色交替 |

### 4.3 历史层架构：三路并行对照实验后采纳"单点收口"

**方法**：三个 worktree 并行实现三种方案，用**统一判据**（`tests/test_history_architecture.py`）对照：

```
              AR-stop(硬)  SOFT1(软)   全套        兜底点  档次
main               ❌ 红        ❌ xfail   206/1f        7     fail
路线1 managed      ❌ 红        ❌ xfail   206/1f(含1回归) —    fail
路线2 projected    ❌ 红        ❌ xfail   206/1f        7→5   fail
路线3 settle       ✅ 绿        ❌ xfail   207/1xfail   7→8   第二档
路线5 atomic       ✅ 绿        ✅ 通过    208/0         7    第一档 ★
```

**最终采纳路线 3**，理由是**用户推翻了我的优先级判断**：

> "路线3的'正在执行工具时 ctrl+c，能捞回已完成的工具结果'同样很重要。"

**我承认算错了**：拿罕见事件（强杀/断电）的收益换高频操作（Ctrl+C）的损失，且"重跑一次就好"的前提不成立——**工具未必幂等**。

**教训**：权衡要比"频率 × 单次代价"，不能只比哪种更优雅。

### 4.4 跨组件冲突审计

修掉 4 处互相矛盾的设计（`ed2b234`），包括：压缩器重写历史与新增组件的冲突、`/history del` 删单条导致兄弟 `tool` 变孤儿等。

---

## 五、文档

| 文档 | 说明 |
|---|---|
| `docs/design.html` / `docs/design-zh.html` | **总设计文档**（中英双版，11 章）。原 Phase 0 文档重命名为 `design-phase0*.html` 归档 |
| `docs/design-zh.html` **§2** | **新增章节：「判断的归属：哪些给代码，哪些给模型」**——本项目最核心的架构原则 |
| `docs/hermes-vs-dummy-tool-loop.md` | Hermes 与 dummy 工具循环逐项对照（457 行，全部带 `file:line`） |
| `docs/p1b-verification-design.md` | 验证严格度调研（三轴框架、六家对照、L1~L5 阶梯） |
| `docs/tool-result-structuring-design.md` | 工具结果结构化设计（277 行） |
| `docs/research-structured-tool-results.md` | 学术文献调研（9 篇论文 + 证据强度分级） |
| `docs/tool-result-success-failure-survey.md` | 工程实践调研（427 行，生态答案是"结构化字段 + 纯文本载荷双层并存"） |
| `docs/agent-concurrency-interrupt-survey.md` | **工具并发与打断机制横向调研**（1370 行，覆盖 Claude Code / Codex / Hermes / DeepSeek Harness / OpenHands / OpenCode / OpenClaw 等） |
| `docs/interrupt-semantics-plan.md` | **打断语义改进实施方案**（含文件/行号/改法/验证/影响评估） |
| `pytest.ini` | 新增，让外部工具能识别本项目的规范测试命令 |

---

## 六、当前状态

```
代码规模:  7015 行 Python(不含测试与 scripts)
测试:      255 条(全部通过)
工具:      5 个(terminal / read_file / write_file / web_search / web_extract)
```

**测试数量变化**：119 → 158 → 174 → 182 → 186 → 196 → 207 → 208 → 211 → 231 → 241 → 245 → 254 → 255

---

## 七、这一阶段确立的核心原则

写进 `docs/design-zh.html` §2：

| 类别 | 谁判断 | 例子 |
|---|---|---|
| **协议合法性** | **代码硬保证** | `tool_calls` ↔ `tool` 配对、角色交替（API 强制） |
| **安全确认** | **人** | 执行命令前的确认、写项目外文件前的确认 |
| **语义判断** | **模型** | "任务完成了吗"、"什么算证据"、"这次重复是否合理" |

**判断方法**：问"这个问题是协议/事实，还是语义？"

- 协议/事实 → 代码（可枚举、无歧义）
- 语义 → 模型（枚举不完现实）

**这一阶段的每一处改动，都是这条原则的具体应用。**
