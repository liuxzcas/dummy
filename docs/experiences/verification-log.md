# 关键验证记录(Verification Log)

> 用途:关键决策的验证证据链。验证方法都在仓库可复跑(tests/ 与 scripts/),
> 本文件只记录"何时验证了什么、结果如何、支撑了什么决策"。
> 完整结果汇报仍在会话内;此处只存结论性记录,保持轻量。
>
> 追加规则:涉及核心行为(压缩 / 记忆 / 搜索 / 工具交互)的恢复、重构、
> 或参数调整,验证通过后在此追加一行。

## 2026-08-14

| 验证对象 | 方法 | 结果 | 关联决策 |
|----------|------|------|---------|
| L2 增量摘要质量(恢复前) | `scripts/quality_gate.py`(真实 DeepSeek 8 题) | 8/8 = 100%(门 ≥90%) | 移除 core 遗留的 `enable_l2=False` 覆盖,恢复两级压缩 L1+L2 |
| L2 压缩真实冒烟 | `scripts/smoke_compress.py`(25 轮,压缩触发 7 次) | 事实召回 5/5,调用错误 0,压缩率 69%~77% | 同上 |
| 本地部署统计适配 | hermes-verify 定向 15 项(窗口映射/端点判断/成本文本)+ pytest 72 项 | 15/15 + 72 passed | 本地(ollama/vllm)端点成本显示"本地",窗口按模型解析 |
