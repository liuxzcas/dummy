# Phase 2.2 Step 3 验证结果记录

> 测试时间:2026-08-07
> 测试范围:Step 3(L1 ToolResult 折叠 + 折叠原文归档,决策 C)
> 被测文件:`compressor.py`(L1 折叠与 compress 编排)、`session_store.py`(归档表)
> 测试方式:ad-hoc 验证脚本(hermes-verify- 前缀,逐项逐断言计分,失败不中断)
> 结果:**28/28 通过 (PASS)**

## 验证项目 A — _fold_tool_results 折叠逻辑(13/13)

| # | 断言 | 结果 |
|---|------|------|
| A1 | 输入未被修改(原子性,浅拷贝) | ✅ |
| A2 | 折叠条数 = 2 | ✅ |
| A3 | 捕获原文条数 = 2 | ✅ |
| A4 | c1 原文完整捕获(tool_call_id + 全文 1000 字符) | ✅ |
| A5 | c3 原文完整捕获(907 字符) | ✅ |
| A6 | 折叠后保留头部 200 字符 | ✅ |
| A7 | 折叠后保留尾部 100 字符 | ✅ |
| A8 | 标记指向归档表并含 tool_call_id | ✅ |
| A9 | 标记含原文长度与"已截断" | ✅ |
| A10 | 短 tool 消息(100 字符)不动 | ✅ |
| A11 | 非 tool 长消息(2000 字符 user)不动 | ✅ |
| A12 | c3 头尾保留(头 200 / 尾 100,围绕标记) | ✅ |
| A13 | 消息结构不变(role / tool_call_id 保留) | ✅ |

## 验证项目 B — compress() 编排(9/9)

| # | 断言 | 结果 |
|---|------|------|
| B1 | 压缩成功(success=True) | ✅ |
| B2 | 策略标记 = "L1" | ✅ |
| B3 | 折叠计数 = 1 | ✅ |
| B4 | 折叠原文随结果返回(folded_originals) | ✅ |
| B5 | 压缩后字符数下降 | ✅ |
| B6 | 输入未被修改(原子性) | ✅ |
| B7 | 无活干时 strategy="none" 且成功(非失败) | ✅ |
| B8 | enable_l1=False 时不折叠 | ✅ |
| B9 | enable_l2=True 时显式抛 NotImplementedError(Step 5 守卫) | ✅ |

## 验证项目 C — tool_result_archive 归档(6/6)

| # | 断言 | 结果 |
|---|------|------|
| C1 | 写入后取回 == 原文 | ✅ |
| C2 | 同键重复写入覆盖(幂等) | ✅ |
| C3 | 不存在的键返回 None | ✅ |
| C4 | 空列表 no-op | ✅ |
| C5 | 旧库(无归档表)初始化时自动重建表(迁移) | ✅ |
| C6 | original_len / folded_at 元数据正确 | ✅ |

## 过程中发现并修正的测试脚本问题(与代码无关)

1. 首次运行:c3 原文长度断言写错(904 vs 实际 907 字符)——测试数学错误,修测试
2. 第二次运行:旧库迁移用例构造了错误的结构(缺 session_id 列的伪旧表),导致
   CREATE UNIQUE INDEX 报错——改用"真实 schema 建库后 DROP 归档表"模拟旧库,通过

> 教训:ad-hoc 验证脚本自身的 bug 会误报(与 ad-hoc-verification skill 第 1 条坑一致);
> 迁移类测试要用"真实旧状态"构造,不要用残缺结构。

## 结论

- L1 折叠:截断格式(头 200 + 标记 + 尾 100)、原文捕获、原子性、无操作场景全部正确
- compress() 编排:L1 策略标记、字符缩减、输入安全、阶段开关守卫全部正确
- 归档表:往返一致、幂等、旧库迁移全部正确
- Step 3 验收通过,可进入 Step 4(core.py 触发点接入)
