# 待讨论事项(Discussion Notes)

> 会话中提出的、暂不实施的设计讨论点,集中记录避免遗忘。
> 讨论定稿后移入 roadmap 或设计文档。

## 2026-08-21

### 1. 运行时 workspace 路径与环境选择

**话题**:agent 运行时,能否让用户选择一个 workspace 路径和工作环境?

**待讨论子问题**:
- 用户怎么选?(启动时交互式询问?`/workspace` 命令?配置文件?)
- 选择后影响范围:terminal 工具的 cwd?read_file/write_file 的相对路径基准?技能目录?
- 与现有"项目根 = cwd"假设的关系(真机已暴露:用户改 terminal cwd 后技能路径失效,依赖注入解决了一部分)
- 是否会引入"多项目/多环境"概念(每项目独立记忆?独立技能?)
- 会不会影响现有代码(压缩/记忆/会话均基于当前目录)

### 2. write_file diff 新旧内容标色

**话题**:write_file 的 _show_diff 预览中,旧内容(-)标红、新内容(+)标绿?

**初步评估**:改动很小——diff 行按前缀 +/- 用 colors 的 RED/GREEN 着色(复用现有 colors 系统,DUMMY_COLOR=0 自动禁用);不影响确认逻辑与测试(着色是显示层)。
